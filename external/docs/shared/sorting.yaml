definitions:
  sortCreatedAt:
    name: sortCreatedAt
    in: query
    description: Sort objectives by createdAt field
    required: false
    schema:
      type: string
      enum: [ "asc", "desc" ]
  sortTitle:
    name: sortTitle
    in: query
    description: Sort objectives by title field
    required: false
    schema:
      type: string
      enum: [ "asc", "desc" ]
  sortNotifyAt:
    name: sortNotifyAt
    in: query
    description: Sort objectives by notifyAt field
    required: false
    schema:
      type: string
      enum: [ "asc", "desc" ]
