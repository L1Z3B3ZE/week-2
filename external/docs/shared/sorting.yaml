definitions:
  sortCreatedAt:
    name: sortCreatedAt
    in: query
    description: Sort objectives by createdAt field
    required: false
    schema:
      type: string
      enum: [ "asc", "desc" ]
  sortTitle:
    name: sortTitle
    in: query
    description: Sort objectives by title field
    required: false
    schema:
      type: string
      enum: [ "asc", "desc" ]
  sortNotifyAt:
    name: sortNotifyAt
    in: query
    description: Sort objectives by notifyAt field
    required: false
    schema:
      type: string
      enum: [ "asc", "desc" ]
  sortBy:
    name: sortBy
    in: query
    description: Sort objectives by the specified field (createdAt, title, notifyAt)
    required: false
    schema:
      type: string
      enum: [ "createdAt", "title", "notifyAt" ]
  sortOrder:
    name: sortOrder
    in: query
    description: Sort order (asc or desc)
    required: false
    schema:
      type: string
      enum: [ "asc", "desc" ]